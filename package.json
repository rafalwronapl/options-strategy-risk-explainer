{
  "name": "options-strategy-risk-explainer",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic options strategy risk explainer prototype.",
  "scripts": {
    "check": "node --check app.js && node --check tests/browser-smoke.test.js && node tests/core.test.js && node tests/browser-smoke.test.js",
    "test": "node tests/core.test.js",
    "test:browser": "node tests/browser-smoke.test.js"
  },
  "keywords": [
    "options",
    "risk",
    "options-strategies",
    "education"
  ],
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
